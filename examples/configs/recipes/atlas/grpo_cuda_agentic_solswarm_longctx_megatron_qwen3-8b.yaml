# The Megatron counterpart of grpo_cuda_agentic_solswarm_longctx_qwen3-8b.yaml:
# same 131072-token window, same dataset, same agent stack; only the training
# backend differs, so the two runs are directly comparable. The backend never
# reaches NeMo-Gym — the agent talks to the policy through the vLLM server.
# The parallelism mirrors the DTensor variant: the whole node splits the
# sequence (context parallel 8, tensor parallel 1).
#
# Launch (single B200 endpoint, needs MODAL_PROXY_TOKEN_ID/SECRET):
#   uv run --extra atlas python submit_grpo.py -e agsol_longctx_mcore \
#     --config grpo_cuda_agentic_solswarm_longctx_megatron_qwen3-8b.yaml \
#     --cluster cw-dfw-cs-001 --num-nodes 1 --time 3:00:00 \
#     --enroot-agent-image $W/containers/solswarm-agent-amd64.sqsh
defaults: "grpo_cuda_agentic_solswarm_longctx_qwen3-8b.yaml"

checkpointing:
  checkpoint_dir: "results/grpo-cuda-agentic-solswarm-longctx-megatron"

policy:
  # Megatron refuses context parallelism without packing ("Sequence Packing
  # must be enabled to use Context Parallelism with MCore"). The base recipe
  # turns packing off for a reason that does not apply here: it describes the
  # automodel path, where packing forces flash-attention-2 and its varlen
  # kernel intermittently fails on long agentic trajectories. Megatron packs
  # through Transformer Engine instead, so that instability is not inherited --
  # but it is the thing to suspect first if this run dies in the logprob pass.
  sequence_packing:
    enabled: true
  dtensor_cfg:
    enabled: false
  megatron_cfg:
    enabled: true
    # Required by MCore whenever logprob_chunk_size is set, which the recipe
    # this inherits does to keep the logprob pass over a 131072-token sequence
    # from materializing its activations at once.
    defer_fp32_logits: true
    # The sequence is split across the node; the weights are not.
    tensor_model_parallel_size: 1
    pipeline_model_parallel_size: 1
    context_parallel_size: 8
    sequence_parallel: false
    # Even split 8 ways (16384 tokens of activations per rank), the training
    # forward OOMs on 80 GiB H100s without recomputation: rank 6 hit 72 GiB
    # allocated with 34 MiB free while the colocated vLLM worker holds its
    # share (job 15160276). Recompute activations rather than hold them.
    activation_checkpointing: true

logger:
  log_dir: "logs/grpo-cuda-agentic-solswarm-longctx-megatron"
